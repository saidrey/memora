# Configuración local del proyecto (personal)
# Fuerza el registro público de npm para este proyecto, sobrescribiendo
# la configuración global (JFrog / trabajo) sin modificarla.
registry=https://registry.npmjs.org/
